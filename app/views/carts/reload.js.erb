(function () {
  document.getElementById("aside-cart").innerHTML = "<%= j render "carts/aside_cart", cart: @cart, current_item: @cart_item %>";
  document.getElementById("navbar-cart").innerHTML = "<%= j link_to @cart.items.size, @cart %>";

  // 変更があった行を強調表示する
  // HACK アニメーションできるようにする
  const current = document.getElementById("current-item");
  const currentStyle = window.getComputedStyle(current);
  current.style.backgroundColor = "#88ff88";
  // current.animate({"background-color": ["#88ff88", currentStyle.backgroundColor]}, 1000);
}());
